<%= gmaps("map_options" => {
                             "container_class" => "map_container event_map_container",
                             "class" => "event_map",
                             "detect_location" => false,
                             "auto_zoom" => false,
                             "zoom" => 15
                           },
                           "markers" => { "data" => event.gmaps4rails_address })
%>
